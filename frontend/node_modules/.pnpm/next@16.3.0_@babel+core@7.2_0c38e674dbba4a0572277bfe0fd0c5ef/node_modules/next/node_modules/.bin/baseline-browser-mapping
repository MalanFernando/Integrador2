#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../baseline-browser-mapping/dist/cli.cjs" "$@"
else
  exec node  "$basedir/../../../baseline-browser-mapping/dist/cli.cjs" "$@"
fi
# cmd-shim-target=D:/INSTITUTO/TercerSemestre/Integrador/hastaLaVuelta/frontend/node_modules/.pnpm/next@16.3.0_@babel+core@7.2_0c38e674dbba4a0572277bfe0fd0c5ef/node_modules/baseline-browser-mapping/dist/cli.cjs
