#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../eslint/bin/eslint.js" "$@"
else
  exec node  "$basedir/../../../../eslint/bin/eslint.js" "$@"
fi
# cmd-shim-target=D:/INSTITUTO/TercerSemestre/Integrador/hastaLaVuelta/frontend/node_modules/.pnpm/@eslint-community+eslint-ut_df2de32e1f03df104fd34016f7a8b23c/node_modules/eslint/bin/eslint.js
