#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../typescript/bin/tsserver" "$@"
else
  exec node  "$basedir/../../../typescript/bin/tsserver" "$@"
fi
# cmd-shim-target=D:/INSTITUTO/TercerSemestre/Integrador/hastaLaVuelta/frontend/node_modules/.pnpm/typescript-eslint@8.66.0_es_0237511fa0ea839eefe5e18efca20173/node_modules/typescript/bin/tsserver
