#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../@babel/parser/bin/babel-parser.js" "$@"
else
  exec node  "$basedir/../../../@babel/parser/bin/babel-parser.js" "$@"
fi
# cmd-shim-target=D:/INSTITUTO/TercerSemestre/Integrador/hastaLaVuelta/frontend/node_modules/.pnpm/eslint-plugin-react-hooks@7.1.1_eslint@9.39.5_jiti@1.21.7_/node_modules/@babel/parser/bin/babel-parser.js
