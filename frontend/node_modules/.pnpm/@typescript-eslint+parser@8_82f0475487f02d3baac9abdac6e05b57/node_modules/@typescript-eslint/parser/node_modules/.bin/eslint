#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../eslint/bin/eslint.js" "$@"
else
  exec node  "$basedir/../../../../eslint/bin/eslint.js" "$@"
fi
# cmd-shim-target=D:/INSTITUTO/TercerSemestre/Integrador/hastaLaVuelta/frontend/node_modules/.pnpm/@typescript-eslint+parser@8_82f0475487f02d3baac9abdac6e05b57/node_modules/eslint/bin/eslint.js
